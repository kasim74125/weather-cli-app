# Weather CLI App

A simple command-line application written in **C++** that simulates fetching weather data.  
This project is designed as a demo repository with multiple configuration files.

## 🚀 Features
- Written in C++17
- Reads configuration from `config.json` and `config.yml`
- Simulates weather data output
- Easy to extend with real API integration

## 📦 Build
```bash
g++ main.cpp -o weather-cli
